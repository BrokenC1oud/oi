#include <iostream>
#include <vector>

class Solution {
public:
  static int minimumTotal(std::vector<std::vector<int>> &triangle) {
    std::vector<int> paths(*(triangle.rbegin()));
    for (int i = 0; i < triangle.size() - 1; i++) {
    }
  }
};
